# Abdirizak Ali

# Methods used 
# forward(value): It moves the turtle in forward direction.
# speed(value): It changes the speed of the turtle.
# penup(): Stop drawing.
# pendown(): Start drawing.
# left(value): It moves the turtle towards the left.
# import turtle

import ??????
  
# initialise turtle instance
star = turtle.Turtle()
  
# increases the speed of turtle
star.speed(?)
  
# to change the background color
star.getscreen().bgcolor("?")
star.color("?")
  
# stop drawing
star.penup()
  
# move the turtle 
star.goto((?, ?))
  
# start drawing
star.pendown()
  
# function to draw stars
def stars(turtle, size):
    if size <= ?:
        return
    else:
        for i in range(?):
            
            # moving turtle forward
            turtle.forward(size)
            stars(turtle, size/3)
  
            # moving turtle left
            turtle.left(216)
  
  
# calling the stars function
stars(????, ?)
turtle.done()
